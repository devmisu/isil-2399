<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.main.MainActivity"
        android:background="@color/background">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragmentContainerMain"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/bottomNavMain"
            tools:background="@color/skyblue_light"/>

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavMain"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            style="@style/Widget.BottomNavigationView"
            app:itemHorizontalTranslationEnabled="false"
            app:labelVisibilityMode="unlabeled"
            app:itemRippleColor="@color/skyblue"
            app:itemTextColor="@color/bottom_nav_selector_text"
            app:itemIconTint="@color/bottom_nav_selector_text"
            android:background="@drawable/bg_bottom_nav"
            app:menu="@menu/menu_main_activity"
            tools:ignore="PrivateResource"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>